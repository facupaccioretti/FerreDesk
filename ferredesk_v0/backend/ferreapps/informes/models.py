from django.db import models
from django.conf import settings

# Por ahora no necesitamos modelos adicionales para el informe de stock bajo
# ya que usamos directamente la vista VistaStockProducto existente
# Los modelos se pueden agregar en el futuro si se necesitan informes más complejos
